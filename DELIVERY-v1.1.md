# طلا تریدر (XAU Trader) — نسخه ۱.۱ — تحویل فاز اول

## این چیست؟

یک اکسپرت (Expert Advisor) برای نرم‌افزار متاتریدر ۵ که یک پنل کامل مدیریت سرمایه و ثبت معامله، مستقیماً روی چارت قیمت قرار می‌دهد. این نسخه (۱.۱) پایان فاز اول پروژه است و به‌عنوان یک نسخه کامل و آماده استفاده تحویل داده می‌شود.

فاز بعدی پروژه («استراتژی و سیگنال‌دهی خودکار») جدا و در مرحله‌ی بعد اضافه خواهد شد؛ این نسخه فقط بخش «مدیریت سرمایه و معامله» را در بر می‌گیرد.

## چه کارهایی انجام می‌دهد؟

1. **محاسبه خودکار حجم معامله** — با وارد کردن درصدی از موجودی حساب یا یک مبلغ ثابت دلاری، حجم مناسب معامله بر اساس فاصله تا حد ضرر خودش محاسبه می‌شود.

2. **نمایش نقطه ورود، حد سود و حد ضرر روی خود چارت** — این سه سطح مستقیم روی نمودار قیمت دیده می‌شوند و با کشیدن ماوس روی چارت قابل جابجایی هستند.

3. **سه نوع سفارش** — مارکت (فوری)، لیمیت و استاپ؛ هر سه از همین پنل قابل انتخاب و ارسال هستند.

4. **ثبت معامله با یک کلیک و یک تاییدیه‌ی امنیتی** — دکمه‌ی خرید/فروش برای جلوگیری از کلیک اشتباهی، بار اول فقط آماده می‌شود و با کلیک دوم واقعاً سفارش ارسال می‌شود.

5. **پیشنهاد خودکار حد سود** — به محض تعیین حد ضرر، حد سود به‌صورت پیش‌فرض با نسبت ۱ به ۲ پیشنهاد داده می‌شود (قابل تغییر دستی).

6. **مدیریت معاملات باز** — فهرست همه‌ی معاملات باز (سود/زیان لحظه‌ای، حجم، قیمت ورود) با امکان بستن هرکدام؛ حتی اگر چند معامله هم‌زمان باز باشد.

7. **تغییر حد سود/ضرر معاملات باز، مستقیم روی چارت** — با کشیدن خط، بدون نیاز به باز کردن پنجره‌ی جدید.

8. **بازیابی خودکار معامله‌ی باز** — اگر تایم‌فریم چارت عوض شود یا ترمینال بسته و باز شود، اکسپرت معامله‌ی باز موجود را خودش دوباره پیدا و نمایش می‌دهد؛ چیزی گم نمی‌شود.

9. **شخصی‌سازی کامل ظاهر** — تغییر تم روشن/تیره، اندازه‌ی پنل، و زبان (فارسی/انگلیسی).

10. **حفظ تنظیمات** — تنظیمات ظاهری و موقعیت پنل، بعد از بستن و باز کردن مجدد ترمینال هم باقی می‌مانند.

## جمع‌بندی

نسخه‌ی ۱.۱ یک ابزار مستقل و کامل برای مدیریت سرمایه و ثبت/مدیریت معاملات است — آماده برای استفاده‌ی واقعی. بخش استراتژی و سیگنال‌دهی، طبق برنامه‌ی پروژه، در فاز جداگانه‌ی بعدی اضافه می‌شود.
