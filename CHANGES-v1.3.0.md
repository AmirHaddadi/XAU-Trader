**تغییرات نسخه ۱.۳.۰**  
خلاصه‌ی همه‌ی چیزهایی که در این نسخه به وب‌اپ اضافه یا اصلاح شد.  
1. امکان سوییچ بین نماد طلا (XAUUSD) و دو نماد کریپتو (BTCUSD و ETHUSD) اضافه شد — دقیقاً همان‌جایی که قبلاً فقط اسم نماد نمایش داده می‌شد، زیر هدر. هدف اصلی این قابلیت تست و توسعه در روزهای تعطیلی بازار طلا (شنبه/یکشنبه) است، چون بازار کریپتو تعطیلی ندارد.  
2. با تعویض نماد، قیمت‌ها، پوزیشن‌های باز، و تاریخچه‌ی معاملات هم به همان نماد فعال محدود می‌شوند — دقیقاً مثل قبل که فقط یک نماد نمایش داده می‌شد، فقط الان قابل انتخاب است.  
3. یک باگ واقعی هم در همین مسیر پیدا و برطرف شد: نوع اجرای سفارش (Filling Mode) قبلاً فقط یک‌بار در ابتدای اتصال اکسپرت، بر اساس نماد چارت تنظیم می‌شد؛ الان برای هر سفارش، بر اساس نماد واقعی همان سفارش تنظیم می‌شود — چون بروکر ممکن است برای طلا و کریپتو نوع اجرای متفاوتی نیاز داشته باشد.  
4. دکمه‌ی روشن/خاموش کردن کراس‌هیر (خطوط راهنمای نقطه‌چین افقی/عمودی روی چارت) دقیقاً کنار دکمه‌ی مگنت در نوار ابزار چارت اضافه شد.  
5. فاوآیکون و لوگوی جدید در همه‌جای وب‌اپ فعال شد: تب مرورگر، آیکون PWA، و لوگوی هدر بالای صفحه (به‌جای آیکون قبلی).  
6. همان لوگو روی اکسپرت متاتریدر (آیکون Navigator) و روی نصب‌کننده‌ی ویندوز (آیکون خود setup، میانبر دسکتاپ، و میانبر Start Menu) هم اعمال شد.  
   
پایان  
