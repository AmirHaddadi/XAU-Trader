# XAU-Trader — راهنمای شروع

این فایل خلاصه‌ای است از این‌که پروژه الان دقیقاً چیست، چه چیزی ساخته شده، چطور اجرایش کنی، و چه نکاتی قبل از تست واقعی باید بدانی. برای جزئیات فنی/تصمیم‌های معماری به `Evolved-FullStack-NewStack.md` (درخواست اولیه) و پلن تایید‌شده مراجعه کن.

## پروژه چیست

XAU-Trader الان دو بخش دارد که با هم کار می‌کنند:

1. **اکسپرت متاتریدر (`MQL5/`)** — همان موتور معاملاتی قبلی (محاسبه ریسک/حجم، ارسال/تغییر/بستن سفارش، رصد پوزیشن‌ها) دست‌نخورده مانده. رابط کاربری قدیمی (پنل روی چارت) غیرفعال شده و اکسپرت الان فقط یک دکمه روی چارت دارد: **Launch Platform**.
2. **پلتفرم وب لوکال (`apps/`)** — یک داشبورد کامل در مرورگر: چارت زنده (کندل + قیمت لحظه‌ای)، پنل مدیریت سرمایه با درگ‌کردن خط Entry/SL/TP روی خود چارت، مدیریت پوزیشن‌های باز، ژورنال معاملات بسته‌شده با امکان کامنت، و تب تنظیمات (تم روشن/تیره، زبان فارسی/انگلیسی، مقادیر پیش‌فرض ریسک).

اکسپرت هیچ محاسبه‌ای را در وب تکرار نمی‌کند — همه‌چیز (اعتبارسنجی، حجم، ارسال سفارش) هنوز داخل خود اکسپرت (`CRiskEngine`/`COrderManager`) انجام می‌شود؛ وب فقط رابط کاربری است و از طریق یک سوکت لوکال با اکسپرت حرف می‌زند.

## معماری خیلی خلاصه

```
مرورگر  <--WebSocket-->  Bridge (Node.js)  <--TCP خام (NDJSON)-->  اکسپرت متاتریدر
                              |
                          SQLite (ژورنال + تنظیمات)
```

- **Bridge** (`apps/bridge`) یک سرور Node.js لوکال است. هم با اکسپرت (روی پورت TCP، پیش‌فرض 9443) صحبت می‌کند، هم یک WebSocket برای مرورگر دارد (پیش‌فرض پورت 8787)، هم دیتابیس SQLite محلی برای ژورنال/تنظیمات نگه می‌دارد.
- **Web** (`apps/web`) یک اپ Next.js است که در مرورگر باز می‌شود (پیش‌فرض پورت 8788 در نسخه‌ی پکیج‌شده).
- **packages/protocol** فرمت پیام‌های رد و بدل شده بین این سه‌تا را تعریف می‌کند (یک منبع مشترک، هم در TypeScript هم دستی در MQL5 پیاده شده).

## وضعیت فعلی — همه‌ی ۴ فاز پلن انجام شده

- **فاز A** — اسکلت Bridge + نمایش زنده‌ی قیمت/پوزیشن‌ها در مرورگر (فقط نمایشی).
- **فاز B** — مدیریت سرمایه کامل از روی وب: تعیین ریسک، درگ‌کردن Entry/SL/TP روی چارت، تایید و ارسال معامله، بستن/تغییر پوزیشن‌ها.
- **فاز C** — ژورنال معاملات بسته‌شده (خودکار، از تاریخچه‌ی متاتریدر) + کامنت روی هر معامله، تب تنظیمات، تم و زبان (فارسی/انگلیسی، با پشتیبانی راست‌به‌چپ).
- **فاز D** — بسته‌بندی یک‌کلیکی: اکسپرت با یک دکمه، سرور Bridge و سرور وب را خودش بالا می‌آورد و مرورگر را باز می‌کند؛ بدون نیاز به نصب Node روی سیستم کارفرما.

همه‌چیز کامپایل/تایپ‌چک تمیز است و با هارنس‌های شبیه‌ساز (چون من امکان تست مستقیم روی ترمینال زنده‌ی متاتریدر یا مرورگر واقعی را ندارم) تست شده. **تست واقعی روی ترمینال زنده و مرورگر هنوز باقی مانده — این قسمت را باید خودت انجام بدهی.**

## چطور در حالت توسعه اجرا کنی (برای تست سریع، بدون پکیج‌کردن)

```bash
pnpm install          # یک‌بار، اگر تازه clone کردی
pnpm dev:bridge        # ترمینال ۱
pnpm dev:web           # ترمینال ۲
```

بعد اکسپرت را روی چارت XAUUSD وصل کن — خودش تلاش می‌کند به Bridge وصل شود (نیازی به کلیک دکمه نیست چون Bridge با `pnpm dev:bridge` از قبل روشن است). بعد مرورگر را خودت به‌صورت دستی به آدرس `http://localhost:3000` باز کن.

### تنظیم مهم قبل از اولین اجرا: در خود متاتریدر

مسیر: **Tools → Options → Expert Advisors**

1. تیک **Allow DLL imports** را بزن (لازم برای دکمه‌ی Launch — بدون این، دکمه و بازکردن خودکار مرورگر کار نمی‌کند).
2. در قسمت **Allow WebRequest for listed URL**، آدرس `127.0.0.1` را اضافه کن (لازم برای اتصال سوکت اکسپرت به Bridge — بدون این، اتصال با خطای بی‌صدا شکست می‌خورد).

این دو تنظیم فقط یک‌بار لازم است و بعدش دیگر نیازی به تکرار ندارد.

## چطور نسخه‌ی نهایی (یک‌کلیکی) را بسازی

```bash
pnpm package
```

این دستور همه‌چیز را می‌سازد و در `build/package-windows/out/win/` و `.../out/linux/` می‌گذارد (هرکدام کاملاً مستقل و آماده‌ی اجرا، بدون نیاز به نصب Node روی سیستم مقصد). داخل هر پوشه:

- `xautrader-bridge.exe` (یا بدون پسوند در لینوکس) — همان Bridge، پکیج‌شده.
- `web/` — سرور وب آماده.
- `node.exe` (یا `node`) — یک Node قابل‌حمل، فقط برای اجرای سرور وب.

برای تست روی سیستم کارفرما: کل پوشه‌ی `out/win/` را کپی کن، و در اکسپرت (پارامترهای ورودی) مسیر کامل `xautrader-bridge.exe` را در `InpBridgeExePath` وارد کن. بعد با کلیک روی دکمه‌ی Launch Platform، خودش Bridge را بالا می‌آورد، صبر می‌کند وصل شود، و مرورگر را باز می‌کند.

**این مسیر (اجرای exe پکیج‌شده زیر Wine روی همین سیستم لینوکسی، نزدیک‌ترین شبیه‌سازی که این‌جا در دسترس بود) کامل تست و تایید شد — ولی روی یک ویندوز واقعی هنوز تست نشده.**

## نکات مهم / محدودیت‌های شناخته‌شده

- **بازشدن خودکار مرورگر** (`ShellExecuteW` با "open" روی یک URL) از نظر تئوری/کد درست است، ولی چون امکان دیدن مرورگر واقعی را ندارم، این یک مورد را باید خودت با چشم خودت تایید کنی.
- **حد ضرر/حد سود در ژورنال همیشه صفر نمایش داده می‌شود.** متاتریدر پس از بسته‌شدن یک پوزیشن، SL/TP آن را دیگر جایی ذخیره نمی‌کند — این محدودیت خود متاتریدر است، نه باگ. اگر بعداً لازم شد، می‌شود یک کش زنده اضافه کرد که قبل از بسته‌شدن مقدار را نگه دارد.
- **پنل قدیمی روی چارت کاملاً حذف نشده، فقط غیرفعال است** — یک خط کامنت در بالای `XAU_Trader.mq5` (`XAUT_LEGACY_PANEL`) هست که اگر لازم شد به‌سرعت پنل قدیمی را برگرداند (برای rollback اضطراری). بعد از این‌که مطمئن شدی نسخه‌ی جدید کامل جواب می‌دهد، می‌توانیم این کد قدیمی را کامل پاک کنیم.
- **حجم پوشه‌ی پکیج‌شده حدود ۵۰۰ مگابایت است** (به‌خاطر Node قابل‌حمل + کتابخانه‌های React/Next). برای یک ابزار دسکتاپ معاملاتی معمولاً مشکلی نیست، ولی اگر خیلی زیاد به‌نظر می‌رسد بگو تا بهینه‌ترش کنم.
- **از `node:sqlite` استفاده شده** (قابلیت داخلی و رایگان خود Node، نه یک پکیج جانبی) — یک تغییر نسبت به پلن اولیه (که `better-sqlite3` پیش‌بینی شده بود)، چون در تست پکیج‌کردن مشخص شد این گزینه بدون هیچ ریسکی کار می‌کند و کلاً از دردسر "native module" خلاص می‌شویم. یک هشدار بی‌ضرر در لاگ می‌آورد که می‌گوید "experimental" است — مشکلی ایجاد نمی‌کند.
- **ژورنال فعلاً همه‌ی معاملات بسته‌شده‌ی نماد را نشان می‌دهد**، نه فقط آن‌هایی که از همین اکسپرت ارسال شده (چون مدیریت پوزیشن از اول هم همین‌طور بوده). اگر می‌خواهی فقط معاملات خود اکسپرت باشد بگو.

## کارهای بعدی که پیشنهاد می‌کنم

1. خودت اکسپرت را روی یک حساب دمو وصل کن و کل مسیر توسعه (`pnpm dev:bridge` + `pnpm dev:web`) را امتحان کن — این سریع‌ترین راه برای دیدن این‌که همه‌چیز واقعاً کار می‌کند.
2. بعد از تایید حالت توسعه، `pnpm package` را بزن و همان پوشه‌ی خروجی را روی یک ویندوز واقعی (یا حتی همین سیستم، چون نسخه‌ی لینوکسش هم ساخته می‌شود) امتحان کن.
3. اگر همه‌چیز خوب بود، می‌رویم سراغ پاک‌کردن کامل کد پنل قدیمی و یک دور جلا/پالیش نهایی روی ظاهر وب (چون این‌جا من امکان دیدن واقعی رابط در مرورگر را نداشتم).

هر سوالی بود یا جایی جواب نداد، دقیقاً همان چیزی که دیدی را برایم بنویس (مثل قبل) تا سریع پیدا کنم مشکل کجاست.
