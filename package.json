{
  "name": "xau-trader-platform",
  "private": true,
  "version": "0.1.0",
  "description": "XAU-Trader local web platform — Node bridge + Next.js client, driven by the MT5 Expert Advisor over a local socket.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "pnpm --filter @xau-trader/web dev",
    "dev:bridge": "pnpm --filter @xau-trader/bridge dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
