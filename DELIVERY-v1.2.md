**طلا تریدر (XAU Trader) — نسخه ۱.۲ — به‌روزرسانی بر اساس فیدبک شما**  
**این چیست؟**  
این فایل، تغییرات نسخه ۱.۲ نسبت به تحویل قبلی (نسخه ۱.۱) را خلاصه می‌کند. تمام موارد زیر مستقیماً بر اساس فیدبکی که برای پنل ارسال شد انجام شده‌اند.  
**چه چیزهایی درست/اضافه شد؟**  
1. **مشکل جابجا نشدن خط استاپ و حد سود با ماوس، برطرف شد** — یک باگ واقعی در نرم‌افزار باعث می‌شد کشیدن خط‌ها روی چارت گاهی اصلاً کار نکند؛ ریشه‌ی مشکل پیدا و کاملاً رفع شد.  
2. **دکمه‌ی «تایید نهایی» اضافه شد** — با زدن دکمه‌ی خرید یا فروش، یک کادر خلاصه‌ی معامله (جهت، حجم، قیمت ورود، حد سود، حد ضرر) باز می‌شود با دو دکمه‌ی مشخص «تایید نهایی» و «انصراف». معامله فقط با زدن «تایید نهایی» واقعاً ارسال می‌شود؛ دیگر خبری از تاییدِ خودکار با یک تایمر کوتاه نیست.  
3. **نسبت ریسک به ریوارد (R/R) قابل تنظیم شد** — یک کنترل +/− روی پنل اضافه شده که نسبت حد سود به حد ضرر را بین ۱ به ۱ تا ۱ به ۵ تنظیم می‌کند (پیش‌فرض ۱ به ۲).  
4. **پیش‌فرض، هیچ خطی روی چارت نیست** — خط‌های ورود/حد سود/حد ضرر فقط از لحظه‌ای که شروع به تنظیم یک معامله می‌کنید روی چارت ظاهر می‌شوند، و بعد از ارسال معامله دوباره پاک می‌شوند — دیگر از همان لحظه‌ی اتصال اکسپرت روی چارت نمی‌مانند.  
5. **اعداد ورود/حد سود/حد ضرر از روی پنل حذف شدند** — این اعداد دیگر به‌صورت فیلد عددی روی پنل نیستند؛ تنظیم آن‌ها فقط با کشیدن خط روی چارت انجام می‌شود، و فقط داخل همان کادر «تایید نهایی» (لحظه‌ی ارسال معامله) نمایش داده می‌شوند.  
6. **فاصله‌ی پیش‌فرض حد ضرر کاهش یافت** — فاصله‌ی پیشنهادی اولیه‌ی حد ضرر به مقداری کوچک‌تر و منطقی‌تر تغییر کرد؛ فاصله‌ی زیاد قبلی برطرف شد.  
**جمع‌بندی**  
نسخه‌ی ۱.۲ همان ابزار مدیریت سرمایه و معامله‌ی نسخه‌ی ۱.۱ است، با اصلاح تعامل با ماوس روی خطوط و یک مسیر ساده‌تر و امن‌تر برای تایید نهایی معامله. آماده‌ی تست مجدد است.  
