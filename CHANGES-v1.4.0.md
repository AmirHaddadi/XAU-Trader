**تغییرات نسخه ۱.۴.۰**  
   
 خلاصه‌ی همه‌ی چیزهایی که در این فاز به وب‌اپ اضافه یا اصلاح شد.  
1. عناصر رسم‌شده روی چارت (خط روند، خط افقی، خط عمودی، مستطیل، فیبوناچی) حالا قابل جابه‌جایی هستند — با کلیک و کشیدن روی خود شکل، کل آن جابه‌جا می‌شود؛ برای خط روند/مستطیل/فیبوناچی با کشیدن هر کدام از دو سرِ شکل هم می‌شود فقط همان نقطه را اصلاح کرد (تغییر شکل، بدون جابه‌جایی کل شکل).  
2. آخرین رنگی که برای یک عنصر رسم‌شده انتخاب می‌کنید، از این به بعد برای عنصر بعدی که رسم می‌کنید هم به‌طور خودکار استفاده می‌شود — دیگر نیازی نیست هر بار رنگ را دستی دوباره تنظیم کنید. این رنگ ذخیره می‌شود و بعد از بستن و باز کردن دوباره‌ی برنامه هم باقی می‌ماند.  
3. هدر بالای صفحه کاملاً بازطراحی شد: منوی داشبورد/ژورنال/تنظیمات به یک منوی همبرگری جمع‌وجور تبدیل شد (به‌جای سه دکمه‌ی همیشه‌بازی که فضا می‌گرفتند)، و موجودی/اکوییتی/مارجین آزاد/بید/اسک/اسپرد/سوییچ نماد همگی در یک ردیف واحد و فشرده کنار هم قرار گرفتند. دیگر یک ردیف کامل جدا فقط برای این اعداد وجود ندارد، و فضای آزادشده به چارت داده شد.  
4. فاصله‌گذاری و چیدمان بخش «مدیریت سرمایه» جمع‌وجورتر و مرتب‌تر شد، هم در حالت عادی و هم وقتی این پنل با ریسایز باریک‌تر می‌شود (مورد ۷).  
5. دکمه‌ی تمام‌صفحه به هدر اضافه شد — با زدنش کل صفحه در مرورگر تمام‌صفحه می‌شود و هدر، پنل مدیریت سرمایه، و لیست پوزیشن‌های باز به‌طور کامل جمع می‌شوند تا تمام فضا در اختیار چارت باشد. کافی است ماوس را به لبه‌ی بالا، راست یا پایین صفحه ببرید تا همان بخش با یک انیمیشن نرم دوباره ظاهر شود.  
6. لیست پایین صفحه به دو تب جدا تقسیم شد: «پوزیشن‌های باز» و «سفارش‌های در انتظار». سفارش‌های در انتظار (لیمیت/استاپ) که هنوز اجرا نشده‌اند، از این به بعد به‌صورت زنده روی داشبورد دیده می‌شوند — با امکان لغو هرکدام با یک دکمه، بدون نیاز به مراجعه به متاتریدر. این قابلیت هم سمت اکسپرت و هم سمت وب‌اپ به‌طور کامل پیاده‌سازی شد.  
7. ستون‌های جدول پوزیشن‌ها و سفارش‌ها از این به بعد همیشه نمایش داده می‌شوند، حتی وقتی چیزی برای نمایش نیست — به‌جای خالی‌شدن کامل جدول، یک آیکون و پیام مناسب وسط جدول نشان داده می‌شود.  
8. امکان ریسایز کردن (تغییر اندازه با کشیدن ماوس) اضافه شد: بین چارت و پنل مدیریت سرمایه (چپ/راست)، و بین چارت و لیست پوزیشن‌ها/سفارش‌ها (بالا/پایین). اندازه‌ی انتخابی هم ذخیره می‌شود و در دفعات بعدی باز کردن برنامه حفظ می‌ماند.  
پایان  
